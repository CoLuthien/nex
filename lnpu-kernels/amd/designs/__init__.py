"""Designs we bake instead of the upstream ones.

An entry here exists because the upstream design is wrong on hardware in a way we could not work
around from the instruction stream. Each carries the upstream file verbatim apart from the fix,
so that a diff against IRON shows the change and nothing else.
"""
